[project]
name = "pyside-mcp"
version = "0.1.0"
description = "Servidor MCP para depuración de aplicaciones PySide6 en ejecución"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.0.0",
    "PySide6>=6.5.0",
]

[project.scripts]
pyside-mcp = "main:main"
